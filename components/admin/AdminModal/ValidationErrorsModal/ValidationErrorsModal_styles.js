import styled from "styled-components"

export const ErrorEntry = styled.div`
	font-size: 16px;
	line-height: 19px;
	letter-spacing: -0.005em;
	color: #000000;
	opacity: 0.5;
`
export const ErrorEntryButton = styled.span`
    text-decoration: underline;
    cursor:pointer;
    :hover{
        opacity: 0.6;
    }
`